#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"
# Pure GTK native lock (keyboard + pointer seat grab). Legacy WebKit: lock_shell.py
exec /usr/bin/python3 "$ROOT/desktop/native_lock.py" "$@"
